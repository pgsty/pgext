## 用法

来源：

- [官方项目页面](https://github.com/greenplum-db/gpdb-archive)

`pxf` — 历史上的 Greenplum 扩展，用于通过 PXF 访问未管理的数据。当 PostgreSQL 需要通过外部数据接口访问相应的外部数据源时，请使用此扩展。上游将其描述为一个正在进行中的项目。

### 核心工作流

此组件在审查的源代码中没有确认的独立工作流程 `CREATE EXTENSION`。仅通过上游机制进行构建、加载或启用，并在隔离数据库中验证最终服务器的行为。

### 要求与注意事项

- 该目录记录了版本 `1.0`。
- 上游将该项目描述为一个正在进行中的项目。
- 在生产使用前，请确认权限、支持的 PostgreSQL 版本、升级行为以及失败情况，以匹配固定源代码。
