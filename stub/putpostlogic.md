## Usage

Sources:

- [Official extension control file](https://github.com/rektide/putpostlogic/blob/0d71733aa77085b4f62c49c0bba972c241d0fcbc/putpostlogic.control)

`putpostlogic` — Unreleased logical decoding output-plugin prototype that emits JSON changes through PostgreSQL, Kafka, or Nanomsg.

The reviewed catalog snapshot records version `0.1.0`, kind `headless`, and implementation language `C`.

This component has no standalone extension DDL in the curated record; integrate it only through the upstream-documented load or provider workflow.

Before production use, review the linked control/SQL or provider documentation, verify privileges and compatibility, and test the actual API and failure behavior on the target PostgreSQL build.
