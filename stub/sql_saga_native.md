## Usage

Sources:

- [Official upstream README](https://github.com/veridit/sql_saga/blob/9e39fcc7fc862fcb793f59d996eb01268cf9e822/README.md)

`sql_saga_native` — Rust/pgrx companion library that accelerates sql_saga temporal_merge planning and executor introspection.

The reviewed catalog snapshot records version `0.1.0`, kind `headless`, and implementation language `Rust`.
Install and validate the declared extension dependencies first: `sql_saga`.
The curated compatibility set is `18`; confirm the exact build against the target server.

This component has no standalone extension DDL in the curated record; integrate it only through the upstream-documented load or provider workflow.
The official material contains an experimental, deprecated, unsupported, or explicit warning boundary; read it in full and test failure cases before non-lab use.

Before production use, review the linked control/SQL or provider documentation, verify privileges and compatibility, and test the actual API and failure behavior on the target PostgreSQL build.
